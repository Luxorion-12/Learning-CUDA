# MXFP8 / NVFP4 低精度软件模拟与反量化项目总结


## 1. 项目目标

本项目在不依赖原生 FP8/FP4 指令的普通 NVIDIA CUDA GPU 上，实现 MXFP8 和 NVFP4 的软件量化与反量化。程序读取 FP32 或 FP16 行主序矩阵及量化参数文件，在 GPU 上完成 scale 计算、低精度编码、NVFP4 四位打包、解包和反量化，并输出低精度权重、FP16/BF16/FP32 恢复张量、误差指标及性能日志。

CPU reference 用于定义数值语义和充当 CUDA 结果 oracle；正式计算路径由 CUDA kernel 完成。当前实测设备为 NVIDIA GeForce RTX 3050 Laptop GPU，Compute Capability 8.6，CUDA Toolkit 12.8。

## 2. 低精度格式与数值规则

### 2.1 MXFP8

MXFP8 每 32 个连续元素共享一个 E8M0 scale，每个归一化元素保存为一个 E4M3 编码字节。行主序矩阵先连续展平，量化块允许跨越行边界。

对块内最大绝对值 `amax`，选择能够覆盖该块动态范围的 E8M0 scale `S`：

```text
q_i = encode_e4m3(x_i / S)
x_hat_i = decode_e4m3(q_i) * S
```

block mode 每 32 个元素保存一个 scale；tensor mode 整个张量共享一个 scale。

### 2.2 NVFP4

NVFP4 元素使用 E2M1 四位编码，整个张量共享 FP32 global scale，每 16 个连续元素使用一个 E4M3 local scale。软件实现采用：

```text
G = tensor_amax / (448 * 6)
L_b = encode_e4m3((block_amax / 6) / G)
q_i = encode_e2m1(x_i / (G * decode_e4m3(L_b)))
x_hat_i = decode_e2m1(q_i) * G * decode_e4m3(L_b)
```

两个 E2M1 元素打包进一个字节，高四位保存偶数元素，低四位保存奇数元素；奇数长度最后一个低四位补零。tensor mode 只使用一个 local scale。

### 2.3 舍入

`nearest` 使用最近值与 ties-to-even；`stochastic` 根据相邻两个可表示值之间的位置和固定 seed 选择编码。相同输入与 seed 可复现，不同 seed 能产生不同合法结果。

## 3. 文件和配置协议

输入张量使用固定 20 字节 header：两个 little-endian `int64` 维度和四字节 ASCII dtype，后接 FP32 或 FP16 行主序数据。FP16 输入保留原始二进制位模式，并由 CUDA kernel 原生读取。

量化参数文件支持 `format`、`block_size`、`scale_mode`、`output_type`、`rounding`、`target_gpu` 和可选随机种子。低精度权重文件采用版本化 `LUXQ0001` header，保存格式、矩阵形状、输入/输出类型、scale mode、元素数、global scale、scale 数组和 packed data，可以脱离原输入执行独立反量化。

## 4. 八个优化方向的选择依据与总体流程

本项目没有按照“把常见 CUDA 优化技术全部使用一遍”的方式选择优化方案，而是从程序实际的数据处理流程出发，对量化和反量化 kernel 中可能影响性能的位置逐层分析，再选择与这些位置直接对应的优化方向。

### 4.1 从数据处理流程确定优化位置

MXFP8 和 NVFP4 的主要处理流程可以概括为：

```text
输入 FP32 / FP16
        ↓
Global Memory 读取
        ↓
线程组织与任务分配
        ↓
求 amax / 计算 scale
        ↓
E4M3 / E2M1 编码
        ↓
NVFP4 packed storage
        ↓
Global Memory 写回
        ↓
反量化读取低精度数据和 scale
        ↓
decode × scale
        ↓
FP32 / FP16 / BF16 输出
```

因此，性能问题主要可能出现在四个层面：

```text
执行配置
├─ Thread / Block 数量是否合适
└─ GPU 是否能够保持足够的并行度

内存访问
├─ Global Memory 是否连续访问
├─ 是否存在大量窄 load/store
└─ 是否存在重复读取

线程间协作
├─ amax 规约是否需要大量同步
├─ Shared Memory 是否能够复用数据
└─ Shared Memory 是否存在 Bank Conflict

线程内部计算
├─ 编码过程是否存在复杂分支
├─ 是否存在较长的串行搜索
└─ 固定循环是否能够展开并增加 ILP
```

基于以上分析，最终选择了八个优化方向。

### 4.2 八个方向与瓶颈的对应关系

| 方向 | 主要关注位置 | 选择原因 |
|---|---|---|
| 1. Thread / Block 配置 | Kernel 启动与线程调度 | threads-per-block 会影响 warp 数量、Occupancy 和调度效率，是成本较低、较基础的配置优化 |
| 2. 合并访存 | Global Memory | 量化和反量化需要连续读取大量输入、编码和 scale，因此首先需要确认相邻线程是否能够形成有效的合并访问 |
| 3. 向量化访存 | Global Memory → Register | 即使地址连续，1-byte code 等窄访问仍可能造成一次内存事务中有效字节比例较低，因此进一步测试一次处理多个连续元素 |
| 4. Warp Shuffle | Block 内 amax 规约 | MXFP8 和 NVFP4 都需要计算块内最大绝对值，规约涉及线程间通信，因此测试使用寄存器级 Warp Shuffle 减少 Shared Memory 和同步 |
| 5. Shared Memory 复用 | scale 等重复数据 | 多个线程可能读取相同或相邻 scale，因此测试是否可以将重复数据缓存到 Shared Memory，减少 Global Memory 访问 |
| 6. Bank Conflict | Shared Memory | 使用 Shared Memory 后需要继续检查不同线程的地址是否映射到同一个 bank，避免 Shared Memory 访问串行化 |
| 7. 分支与搜索路径优化 | E4M3 / E2M1 编码 | v0 编码器通过线性扫描寻找最近的低精度候选值，计算次数较多，因此将其改为固定步数二分查找 |
| 8. ILP / 循环展开 | 单线程指令流 | v7 的二分查找循环次数已经固定，因此可以进一步展开循环，减少循环控制指令，并给编译器更多指令调度空间 |

### 4.3 优化实施流程

每个优化方向均按照相同流程进行，而不是直接叠加全部优化：

```text
建立正确的 v0 baseline
        ↓
分析当前数据流和可能的性能瓶颈
        ↓
一次只选择一个优化方向
        ↓
生成独立候选版本
        ↓
CPU oracle / 文件结果正确性验证
        ↓
相同输入和相同计时协议下进行性能测试
        ↓
与上一最佳版本比较
        ↓
        ├─ 有收益且无明显回退 → 晋级
        │
        └─ 无收益或出现回退 → 淘汰
                         ↓
                  进入下一方向
```

整个优化过程始终保持三个原则：

1. **正确性优先于性能。** 每次改变 kernel 后先检查 scale、编码、packed data 和恢复结果，再进行性能比较。
2. **一次只改变一个主要变量。** 避免同时加入多个优化后无法判断性能变化来自哪个因素。
3. **以实测结果决定是否保留。** Shared Memory、Warp Shuffle 等技术并不一定适合当前 kernel，如果实测没有收益，就保留实验记录但不进入最终版本。

最终形成的实际晋级链为：

```text
v0
 ↓
v1 Thread / Block
 ↓
v3 Vectorized Memory
 ↓
v6 Bank Conflict
 ↓
v7 Branch / Search Reduction
 ↓
v8 ILP / Loop Unroll
```

其中 v2、v4 和 v5 分别对应合并访存、Warp Shuffle 和 Shared Memory 复用。它们仍然保留实验源码和分析结果，但由于没有形成明确收益，因此没有叠加到后续最佳版本中。

这种方式能够将每一轮性能变化与具体代码修改对应起来，并避免因为同时叠加多个优化而无法判断实际收益来源。


## 5. 软件架构与执行流程

- `cpu_reference/`：格式编解码、CPU 量化/反量化、文件 IO、配置解析和误差指标。
- `gpu_baseline/`：CUDA kernel、输入生成器、benchmark、正式 CLI 和 CPU/GPU 对照测试。
- `input/`：冻结的 FP32/FP16 测试输入、manifest 和量化配置。
- `optimization/`：v0–v8 独立源码、实验结论和最终对照结果。

正式 CLI 的 `run` 流程为：读取并校验输入与配置，上传原生 FP32/FP16 数据，执行 GPU 量化，保存 `weights.luxq`，执行 GPU 反量化，保存恢复张量并计算误差和性能指标。`dequantize` 流程只读取 `weights.luxq` 并恢复张量。

## 6. 正确性验证

CPU reference 和 CUDA 路径逐层比较 MXFP8 scale/code、NVFP4 global/local scale 与 packed byte、恢复结果及零符号，并覆盖 tensor/block scaling、nearest/stochastic、奇数元素、非整齐尾块、全零、异常值、非法文件和独立反量化。

当前 CTest 共 13 项，全部通过。v8 四个正式流程生成的权重和恢复张量与正确性基线逐字节一致。

## 7. 输入数据与计时协议

冻结输入包含 uniform、normal、outliers、zero-heavy、multiscale 和 `1023×1025` 尾部矩阵，每类同时提供 FP32 与 FP16，共 12 个文件。manifest 记录 seed、形状、dtype、分布和统计量；benchmark 不会自动重新生成输入。

kernel 使用 CUDA event 计时，先 warmup 再重复采样，报告 median 和 nearest-rank p95。文件 IO、显存分配和 CPU oracle 不进入 kernel 时间。正式提交前将统一使用同一协议重跑 v0、v8、CPU reference 和最终误差矩阵。

## 8. Baseline 与瓶颈

v0 的反量化采用每线程恢复一个元素；量化编码器从低到高扫描候选低精度值。Nsight Compute 初步分析表明，相邻线程地址连续，但一字节 code 加载和重复 scale 加载降低了 sector 利用率。更主要的量化瓶颈是 E4M3 最多 126 次、E2M1 最多 7 次的候选扫描。

## 9. 优化方法与迭代历程

所有方向先验证字节级正确性，再与上一最佳版本交错计时。用户指定不设置固定百分比门槛：综合实测更快、核心路径没有有意义回退且正确性不变即可晋级。

最终晋级链：`v0 → v1 → v3 → v6 → v7 → v8`。

| 方向 | 假设与修改 | 结果 | 决策 |
|---:|---|---|---|
| 1 | 扫描64/128/256/512 threads-per-block | 128线程综合约+0.57% | 晋级 |
| 2 | 检查全局内存合并访问 | 地址已连续，问题主要是窄加载 | 无代码候选 |
| 3 | MXFP8每线程读取4个code；NVFP4每线程恢复一个packed byte；向量写回 | 反量化综合约1.53x | 晋级 |
| 4 | shared reduction替换为warp shuffle | 综合约+0.02%，MXFP8轻微回退 | 淘汰 |
| 5 | scale预取到shared memory | NVFP4 block明显回退 | 淘汰 |
| 6 | NVFP4 shared code由byte扩为32-bit，消除bank冲突 | NVFP4 block约+0.023% | 晋级 |
| 7 | 线性候选扫描改为固定7/3步二分查找 | 量化综合约3.55x | 晋级 |
| 8 | 完全展开固定二分步骤 | 在v7上量化综合再提升约3.06% | 晋级 |

### 9.1 失败方案和开发问题

方向3第一次实现把 NVFP4 的 `e2 * global * local` 改写成 `e2 * (global * local)`，数学上等价但 FP32 舍入顺序不同，导致输出哈希变化。该候选没有直接接受，而是恢复原运算顺序后重新执行四类哈希和完整流程检查。这说明低精度软件模拟不仅要比较误差范围，还需要固定计算顺序以保证可复现性。

方向4和方向5虽然减少了同步或全局 scale 读取，但端到端 kernel 实测没有收益或出现回退，因此保留源码和记录而未叠加到后续最佳版本。各方向的完整数据和说明见 `optimization/v*/README.md`。

## 10. 当前最终性能

RTX 3050 Laptop GPU 上，v0/v8 交错运行3轮、每轮预热5次并计时20次的初步结果如下：

| 场景 | v0量化 ms | v8量化 ms | 加速 | v0反量化 ms | v8反量化 ms | 加速 |
|---|---:|---:|---:|---:|---:|---:|
| MXFP8 block / FP16 | 12.328944 | 5.456864 | 2.26x | 0.053248 | 0.036272 | 1.47x |
| MXFP8 tensor / FP32 | 113.272831 | 10.684112 | 10.60x | 0.039936 | 0.025600 | 1.56x |
| NVFP4 block / FP32 | 8.883200 | 1.436048 | 6.19x | 0.046592 | 0.036864 | 1.26x |
| NVFP4 tensor / FP16 | 9.499648 | 7.846768 | 1.21x | 0.039936 | 0.024576 | 1.63x |

四场景几何平均量化约3.66x、反量化约1.47x。最终报告还需加入统一协议下的 p95、GB/s、MaxAbs、MAE、MSE、压缩率和 CPU/GPU 加速比。

## 11. 软件模拟与依赖边界

E4M3、E2M1、E8M0 编解码、scale 选择和四位打包均由普通 CUDA C++ 软件实现；没有使用原生 FP8/FP4 Tensor Core，没有依赖 Transformer Engine。FP16 是正式 GPU 输入类型，BF16/FP16/FP32 是反量化输出类型。target_gpu 仅作为实验环境信息写入报告，不参与 CUDA kernel 的选择或硬件专用路径切换


## 12. 构建与复现

默认只构建正式程序和测试；历史源码始终保留，但仅在显式开启时编译：

```powershell
cmake -S . -B build/cuda -G "Visual Studio 17 2022" -A x64 `
  -T "cuda=C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v12.8" `
  -DLUXORION_ENABLE_CUDA=ON -DCMAKE_CUDA_ARCHITECTURES=86
cmake --build build/cuda --config Release
ctest --test-dir build/cuda -C Release --output-on-failure

# 复现v0-v8时追加：-DLUXORION_BUILD_HISTORY=ON
```

正式量化及独立反量化：

```powershell
.\build\cuda\Release\luxorion_cli.exe run `
  input\benchmark\normal_fp16_1024x1024_seed1234.tensor.bin `
  input\quant.cfg output\run_example

.\build\cuda\Release\luxorion_cli.exe dequantize `
  output\run_example\weights.luxq `
  output\run_example\restored_again.tensor.bin
```
## 13. 参考资料

本项目的低精度格式设计、CUDA kernel 实现和性能优化主要参考了 CUDA 官方文档、NVIDIA 低精度格式资料、Open Compute Project 的 Microscaling 规范、InfiniTensor 训练营项目仓库以及训练营 CUDA 课程材料。

### 13.1 InfiniTensor Learning-CUDA

InfiniTensor 训练营 CUDA 方向项目仓库，是本项目题目要求、提交目录和整体任务背景的主要来源。

[InfiniTensor / Learning-CUDA](https://github.com/InfiniTensor/Learning-CUDA)

### 13.2 NVIDIA CUDA Programming Guide

CUDA 官方编程指南，用于理解 Thread、Warp、Block、Grid、SIMT、Global Memory、Shared Memory、同步机制以及 CUDA kernel 的整体执行模型。

本项目中的 Thread / Block 配置、合并访存、Warp Shuffle、Shared Memory 和 Bank Conflict 等优化方向均建立在这些 CUDA 执行机制之上。

[CUDA Programming Guide](https://docs.nvidia.com/cuda/cuda-programming-guide/)

### 13.3 NVIDIA CUDA C++ Best Practices Guide

CUDA 官方性能优化指南，主要用于理解和设计项目中的性能优化流程，包括：

- Kernel profiling
- Thread / Block 配置
- Occupancy
- Global Memory 合并访问
- Shared Memory
- Register Pressure
- Branch Divergence
- Instruction-Level Parallelism

项目在进行每一轮优化时均采用“修改代码—验证正确性—重新测量性能—决定是否保留”的方式，而不是默认某一种 CUDA 技术一定能够带来性能提升。

[CUDA C++ Best Practices Guide](https://docs.nvidia.com/cuda/cuda-c-best-practices-guide/)

### 13.4 NVIDIA Nsight Compute Documentation

Nsight Compute 是本项目分析 CUDA kernel 性能的主要 profiler。

项目主要关注以下分析内容：

```text
Launch Statistics
Memory Workload Analysis
Occupancy
Memory Transactions
Shared Memory
Warp Stall
```

这些指标用于判断 Thread / Block 配置、Global Memory 访问、Shared Memory 使用和 Warp 执行是否构成实际性能瓶颈。

[NVIDIA Nsight Compute Documentation](https://docs.nvidia.com/nsight-compute/)

### 13.5 OCP Microscaling Formats Specification

Open Compute Project 发布的 Microscaling Formats（MX）规范用于理解 MX 系列低精度格式的基本设计思想，包括 block scaling、共享 scale 和低精度元素表示方式。

该规范是理解 MXFP8 中 Microscaling 机制的重要参考资料。

[OCP Microscaling Formats (MX) Specification](https://www.opencompute.org/documents/ocp-microscaling-formats-mx-v1-0-spec-final-pdf)

### 13.6 NVIDIA Transformer Engine：FP8 / FP4 基础资料

NVIDIA Transformer Engine 的低精度格式说明用于理解和核对 E4M3、MXFP8 和 NVFP4 的基本格式、数值范围和缩放关系。

MXFP8 的基本结构可以概括为：

```text
32 个连续元素
+
E4M3 元素编码
+
E8M0 block scale
```

NVFP4 的基本结构可以概括为：

```text
16 个连续元素
+
E2M1 元素编码
+
E4M3 block scale
+
FP32 global scale
```

相关资料还用于核对 E4M3 和 E2M1 的表示范围及低精度缩放方式。

[NVIDIA Transformer Engine：Using FP8 and FP4](https://docs.nvidia.com/deeplearning/transformer-engine/examples/fp8_primer.html)

### 13.7 NVIDIA Transformer Engine：MXFP8

MXFP8 专门资料用于理解其 block scaling 方式。

其基本恢复关系可以表示为：

```text
x = x_fp8 * s_block
```

其中 `x_fp8` 为低精度 E4M3 元素，`s_block` 为一组元素共享的 block scale。

本项目的软件实现使用普通 CUDA C++ 模拟该低精度存储和恢复过程，不依赖硬件原生 FP8 指令。

[NVIDIA Transformer Engine：MXFP8](https://docs.nvidia.com/deeplearning/transformer-engine/features/low_precision_training/mxfp8/mxfp8.html)

### 13.8 NVIDIA Transformer Engine：NVFP4

NVFP4 专门资料用于理解 E2M1 元素格式和 global/local 两级缩放结构。

其基本恢复关系可以表示为：

```text
x = x_e2m1 * s_block * s_global
```

其中：

```text
x_e2m1   = E2M1低精度元素
s_block  = block级E4M3 scale
s_global = 整个张量共享的FP32 global scale
```

本项目进一步按照 4-bit 存储要求，将两个 E2M1 编码打包到同一个字节中。

[NVIDIA Transformer Engine：NVFP4](https://docs.nvidia.com/deeplearning/transformer-engine/features/low_precision_training/nvfp4/nvfp4.html)

### 13.9 训练营 CUDA 课程材料

项目优化方向的选择还结合了训练营 CUDA 阶段课程材料，主要包括：

- 《并行编程与 CUDA 编程入门》
- 《性能模型与逐元素优化》
- 《内存模型与规约优化》
- 《分块与不规则访存》
- 《异步并行——底层控制与系统优化》
- 《量化与工业级调优部署》

这些材料主要用于理解和选择以下优化方向：

```text
Thread / Warp / Block
Global Memory
Memory Coalescing
Vectorized Memory Access
Shared Memory
Reduction
Warp Shuffle
Bank Conflict
Branch Optimization
ILP
Loop Unroll
CUDA Performance Profiling
```